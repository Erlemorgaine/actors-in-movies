<div class="row">
  <div class="col-md-3">
    <ul class="sidebar-list">
      <li class="sidebar-item"><%= link_to "Watched movies", movies_path, class: "sidebar-link active" %></li>
      <li class="sidebar-item"><%= link_to "Movies to watch", movies_path, class: "sidebar-link" %></li>
    </ul>
  </div>
  <div class="col-md-9">
    <div class="panel panel-primary">
      <div class="panel-heading">
        Movies to watch
      </div>
      <div class="panel-body">

        <% @movies.each do |movie| %>
        <div class="row">
          <div class="col-md-7">
            <h4><%= link_to movie.name, movie_path(movie.id) %></h4>
          </div>
        </div>
        <% end %>

      </div>
    </div>

    <%= link_to "Add new movie", new_movie_path, class: "btn btn-primary" %>
  </div>
</div>
